require File.expand_path(File.dirname(__FILE__) + '/../../spec_helper')

describe ActiveRecord::Base do
  describe '::crewait' do
      # it 'defines basic_validation on the hashes based on the classes they come from...'
  end
end